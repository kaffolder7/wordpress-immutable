# Log the real client IP with `mod_remoteip`
# Makes sure Apache log format uses `%a` (the client IP after RemoteIP) so logs/IDS/rate-limit plugins work correctly.
LogFormat "%a %l %u %t \\"%r\\" %>s %b \\"%{Referer}i\\" \\"%{User-Agent}i\\"" combined_remoteip
