# Trim headers + disable directory listings
ServerTokens Prod
ServerSignature Off
<Directory "/var/www/html">
  Options -Indexes
  AllowOverride All
</Directory>

<Files "xmlrpc.php">
  Require all denied
</Files>

# Example: last-resort deny (toggle by env if needed)
# <Location "/wp-login.php">
#   Require all denied
# </Location>
